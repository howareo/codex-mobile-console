# Verification

This directory contains public, reproducible verification notes only. Deployment
logs, local paths, network addresses, process IDs, and configuration snapshots
belong outside the repository.

`rollback.ps1` creates a normal Git revert commit from a clean working tree.
Runtime rollback for the desktop environment remains `scripts/rollback.ps1`.
